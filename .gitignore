# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/

# MCP local configs (generated by setup-mcp.js — contain machine-specific paths)
.cursor/
.windsurf/
.mcp.json

# Node/Logs
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build/Dist
dist/
# Compiled Electron output — always rebuilt by `npm start` / `build:electron` /
# electron-builder, so it's never used fresh. Tracking it bloated diffs and made
# the PR-review bot review compiled JS (flagging pre-existing source as new).
dist-electron/
build/
# …but the desktop's electron-builder resources (entitlements, notarize hook,
# packaging docs) are source, not build output — they must be tracked.
!brainrouter-desktop/build/
!brainrouter-desktop/build/**
out/
brainrouter-desktop/release/
.next/
.open-next/
.wrangler/

# Synthesized at publish time from brainrouter-changelog/ via
# brainrouter-cli/package.json#sync-changelog. Source of truth is the
# top-level brainrouter-changelog/ folder.
brainrouter-cli/changelog/

# Env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# .env backups (may hold secrets) — never commit them
.env.bak
.env.*.bak
*.env.bak

# TypeScript incremental build cache
*.tsbuildinfo

# Per-user tooling state (Claude Code, brainrouter migration leftovers)
.claude/
.brainrouter/
.brainrouter.migrated/
brainrouter/memory.db
brainrouter/mcp_server.log
brainrouter/benchmark/data/longmemeval_s_cleaned.json
brainrouter-benchmark/results/**
!brainrouter-benchmark/results/.gitkeep
brainrouter-benchmark/reports/**
!brainrouter-benchmark/reports/.gitkeep
# Published benchmark results (the rest of reports/ stays scratch).
!brainrouter-benchmark/reports/README.md
!brainrouter-benchmark/reports/memory-comparison.md
!brainrouter-benchmark/reports/0.4.14-recall-delta.md
brainrouter-benchmark/datasets/membench/**
!brainrouter-benchmark/datasets/membench/.gitkeep
brainrouter-benchmark/datasets/longmemeval/**
!brainrouter-benchmark/datasets/longmemeval/.gitkeep
brainrouter-benchmark/datasets/locomo/**
!brainrouter-benchmark/datasets/locomo/.gitkeep
brainrouter-benchmark/datasets/raw/**
!brainrouter-benchmark/datasets/raw/.gitkeep
# benchmark env clones hold copied API keys — never commit them
brainrouter-benchmark/envs/
.env.benchmark_*

# Top-level test/ scratchpad for LLM-scaffolded throwaway demos
# (not the package-local test folders under brainrouter-cli/, brainrouter/, etc.)
/test/

# Local Projects (root-level scratch dir only — NOT app/projects, packages, etc.)
/projects/
/conceptCode/
brainrouter/brain.db
TENCENTDB_AGENT_MEMORY.md

openSrc/*

CodexImplementationPlan.md
CodexTasks.md
BRAINROUTER_TASKS.md
BRAINROUTER_IMPLEMENTATION_PLAN.md
SETUP.md
Tasks.md
BRAINROUTER_IMPROVEMENTS.md
task.md

# Local-only working files — never push these. They hold WIP feature
# specs, master task queues, and handover notes that are useful across
# sessions but personal to the engineer driving the work.
FEATURE_*.md
FEATURE_*.MD
FULL_TASKS.md
FULL_TASKS.MD
HANDOFF_*.md
IMPLEMENTATION_PLAN.md
OPENHUMAN_BRAINROUTER.md
MEMORY_IMPROVEMENT.md
CLI_IMRPOVEMENT.md
COMPARISION.md
040_COMPARISON.md
045_CODEX_CLI_CODE_COMPARISON.md

AWSPrep

# Local-only planning docs (consult, never commit)
FULL_TASKS_*.md
FULL_TASKS_*.MD
walkthrough.md
IMPLEMENTATION_PLAN_*.md
*_CODEX_BRIEF.md
MOTOR_CORTEX_*.md
GREP_SEARCH_*.md
task-*.md

# Local-only research/audit artifacts + scratch (consult, never commit)
audit-*.json
ClaudeCode*.md
.pkg.json
.ci.yml
# Dev-only renderer control-gallery harness (not part of the app)
brainrouter-desktop/preview.html
brainrouter-desktop/src/preview.tsx
deploy/dev/.env
